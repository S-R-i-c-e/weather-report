V1 Wear=ther report
User inputs city name
	if name is already on the list =>	inform user
						retrieve local coordinates
						create weather request
						fetch weather request


	if name is not in the list =>		create coordinate request
						fetch coordinate request

User selects listed city =>	retrieve local coordinates
					create weather request
					fetch weather request

Coordinate request returned
	if unsuccessful	=>		inform user
					clear input panel

	if successful =>		add city name local coordinates
					create list panel					
					create weather request
					fetch weather request

Weather request returned
	if unsuccessful =>	retry

        if successful => 	create weather panels
				render page

